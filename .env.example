# Environment Configuration
# 
# LOCAL DEVELOPMENT: Copy this file to .env and update values
# REPLIT DEPLOYMENT: Add these as Secrets in Replit dashboard
#
# Environment Detection:
# - Development: Uses .env file
# - Staging/Production: Uses Replit Secrets

# Server Configuration
PORT=5001
NODE_ENV=development

# Environment Type (only needed in Replit)
# ENVIRONMENT=staging  # or 'production'

# API Keys (Optional - add as needed)
# OPENAI_API_KEY=your_openai_api_key_here
# OPENROUTER_KEY=your_openrouter_key_here

# Future Configuration
# DATABASE_URL=your_database_url_here

# Replit Auto-Detection Variables (automatically set by Replit)
# REPL_ID=<auto-set-by-replit>
# REPLIT_DB_URL=<auto-set-by-replit>

# =====================================================
# DEPLOYMENT INSTRUCTIONS:
# =====================================================
# 
# FOR LOCAL DEVELOPMENT:
# 1. Copy this file to .env
# 2. Update values as needed
# 3. Run: npm run dev
#
# FOR REPLIT DEPLOYMENT:
# 1. Push code to GitHub
# 2. Import repository in Replit
# 3. Go to Secrets tab in Replit
# 4. Add required secrets:
#    - ENVIRONMENT=staging (or production)
#    - OPENROUTER_KEY=your_key (if using)
#    - Any other API keys
# 5. Click "Run" - it will auto-build and start
#
# The app automatically detects the environment and loads
# configuration accordingly. No code changes needed!